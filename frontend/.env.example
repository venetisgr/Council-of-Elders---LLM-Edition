# Backend URL — required in production (Vercel), not needed in local dev
# In local development, the Vite proxy handles routing to localhost:8000
# In production, set this to your deployed backend URL (e.g., https://your-backend.railway.app)
VITE_BACKEND_URL=
